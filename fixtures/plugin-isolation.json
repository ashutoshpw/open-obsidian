{
  "schema_version": 1,
  "decision": "D15",
  "policy": "no trusted legacy mode; deny direct vault and shared-configuration capabilities and stage approved vault writes through preview approval",
  "capabilities": {
    "allowed": ["vault.read"],
    "staged": ["vault.preview-write"],
    "denied": ["vault.direct-write", "configuration.write", "filesystem.direct", "network.request", "process.spawn", "credentials.read", "dom.privileged"]
  },
  "required_record_fields": ["reproduction", "safeAlternativesAttempted", "scope", "visible"],
  "safe_alternatives": ["mediated vault read", "preview broker", "workflow disabled"],
  "runtime_probe": {
    "command": "bun run audit:plugin-runtime:all",
    "hardest_targets_command": "bun run audit:plugin-runtime",
    "launcher": "node --experimental-permission --no-addons --disable-proto=throw",
    "coverage": "module-load-only",
    "allowed_host_modules": ["obsidian", "@codemirror/*", "@lezer/*", "codemirror", "luxon", "moment", "svelte", "yaml"],
    "denied_host_capabilities": ["filesystem.direct", "network.request", "process.spawn", "credentials.read", "dom.privileged", "code.dynamic"],
    "certification_policy": "Probe results never change pending-runtime dispositions; Electron renderer compatibility and OS-enforced isolation require separate evidence."
  },
  "platforms": ["macOS", "Windows", "Linux"],
  "feasibility": {
    "scope": "mandatory top-25 plugins plus Minimal and Text Extractor dependencies; wider 30-50 discovery research remains separate",
    "corpus_source": "fixtures/plugin-catalog.json",
    "corpus_count": 27,
    "hardest_targets": ["PC03", "PC02", "PC01", "PC08", "PC06", "PC13", "PC10", "PC19"],
    "bypass_tests": [
      {"id": "direct-filesystem", "capability": "filesystem.direct", "expected": "deny"},
      {"id": "native-abi", "capability": "native.abi", "expected": "deny-or-isolated"},
      {"id": "cross-plugin", "capability": "plugin.cross-access", "expected": "deny-or-mediated"},
      {"id": "monkey-patching", "capability": "dom.privileged", "expected": "deny-or-isolated"},
      {"id": "process-spawn", "capability": "process.spawn", "expected": "deny"},
      {"id": "key-access", "capability": "credentials.read", "expected": "deny"},
      {"id": "resource-exhaustion", "capability": "resource.unbounded", "expected": "bounded-or-deny"},
      {"id": "supply-chain", "capability": "artifact.mutation", "expected": "immutable-pin"}
    ]
  }
}
